# Persona-aligned investigation workload C (v2)

Synthetic fixture only; no measured results or private data.

- Family: `debugging`
- Persona/mode: `detective` / `detective-root-cause`
- Matrix classification: `persona-aligned`

Diagnose a synthetic cache-coherency regression from a bounded incident packet containing an expected state, timeline, observations, competing causes, and one missing fact. Score factual fidelity, evidence/inference separation, discrimination value of the next check, calibration, and safety boundaries. `control-voice` renders the exact immutable control artifact; `task-voice` uses the Task solver plus the same tool-disabled Voice renderer and is compared against `control-voice`.
